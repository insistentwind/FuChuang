<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ning.mapper.RelationMapper">
    <select id="getCompanyByWorkId" resultType="com.ning.domain.entity.Company">
        SELECT c.*
        from relation r
                 LEFT JOIN company c ON r.company_id = c.id
        WHERE r.work_id = #{id};
    </select>
    <select id="getUserByCompany" resultType="java.lang.Integer">
        SELECT count(*)
        FROM relation re
                 LEFT JOIN work w ON re.work_id = w.id
                 LEFT JOIN work_user wu ON w.id = wu.work_id
        WHERE wu.user_id = #{userId}
          AND re.company_id = #{companyId};
    </select>
    <select id="getWorkByCompanyId" resultType="com.ning.domain.entity.Work">
        SELECT w.*,r.company_id
        FROM relation r
                 LEFT JOIN `work` w ON r.work_id = w.id
        WHERE r.company_id = #{companyId}
        AND w.del_flag != 1
    </select>
    <select id="getWorkByCategory" resultType="com.ning.domain.entity.Work">
        SELECT *
        FROM relation r
                 LEFT JOIN `work` w ON r.work_id = w.id
        <where>
            w.del_flag != 1
         <if test="companyId != null">
             AND r.company_id = #{companyId}
         </if>
        <if test="companyId != null">
            AND w.classify_id = #{categoryId}
        </if>
        <if test="companyId != null">
            AND w.city_name = #{cityClassifyId}
        </if>

        </where>

    </select>

</mapper>