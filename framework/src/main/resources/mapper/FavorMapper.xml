<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ning.mapper.FavorMapper">


    <select id="getByFavor" resultType="com.ning.domain.entity.Favor">
        SELECT *
        FROM `favor`
        where user_id = #{userId}
          and work_id = #{workId}
    </select>
    <select id="getAllFavorByUserId" resultType="com.ning.domain.entity.Work">
        SELECT w.*
        FROM `favor` AS f
                 LEFT JOIN `work` AS w
                           ON f.work_id = w.id
        WHERE f.user_id = #{id}
        and w.del_flag != 1
    </select>
</mapper>
