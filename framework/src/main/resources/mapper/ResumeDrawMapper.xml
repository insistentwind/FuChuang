<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ning.mapper.ResumeDrawMapper">
    <select id="getDrawByPage" resultType="com.ning.domain.entity.ResumeDraw">
        SELECT rd.*
        FROM resume_draw rd
                 LEFT JOIN resume r ON r.id = rd.resume_id
        WHERE r.public_pool = 0
        LIMIT #{pageSize} OFFSET #{offset}
    </select>
    <select id="getDrawByResumeId" resultType="com.ning.domain.entity.ResumeDraw">
        SELECT rd.*
        FROM resume_draw rd
                 LEFT JOIN resume r ON r.id = rd.resume_id
        WHERE r.id = #{resumeId}
    </select>
</mapper>