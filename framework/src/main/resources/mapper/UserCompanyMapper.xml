<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ning.mapper.UserCompanyMapper">
    <select id="judgePriByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) from user_company uc
                                 LEFT JOIN company c ON uc.company_id = c.id
                                 LEFT JOIN relation r on c.id = r.company_id
        WHERE uc.user_id = #{userId} and r.work_id = #{workId}
    </select>
    <select id="getCompanyIdByUserId" resultType="java.lang.Integer">
        select uc.company_id from user_company uc
        where uc.user_id = #{userId}
    </select>
</mapper>